export const styled = {
  div: (styles) => styles,
  button: (styles) => styles,
  input: (styles) => styles
};

export const keyframes = (name, animation) => animation;

export const css = (styles) => styles;
